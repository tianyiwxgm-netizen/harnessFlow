# 【并发会话 K】3-2 TDD · L1-08 多模态 × 4 + L1-09 韧性+审计 × 5 · 共 9 份

## 背景

你（K 会话）负责 **L1-08 多模态内容处理 × 4 + L1-09 韧性+审计 × 5 = 9 份**。
其他会话并发做 L1-02/03/05/06/07/10，不冲突。

---

## 任务文件（只动 L1-08 + L1-09 两目录）

### L1-08 多模态内容处理（4 份）

```
docs/3-2-Solution-TDD/L1-08-多模态内容处理/
├── L2-01-文档 IO 编排器-tests.md          （2317 行 3-1 源 · 40 IC 引用）
├── L2-02-代码结构理解编排器-tests.md      （3074 行 · 12 错误码 · 99 IC）
├── L2-03-图片视觉理解编排器-tests.md      （1804 行 · 12 错误码 E01-E12 · 87 IC）
└── L2-04-路径安全与降级编排器-tests.md    （2240 行 · 16 错误码 · 含 PATH_TRAVERSAL）
```

### L1-09 韧性+审计（5 份）

```
docs/3-2-Solution-TDD/L1-09-韧性+审计/
├── L2-01-事件总线核心-tests.md            （2079 行 3-1 源 · 37 错误码）
├── L2-02-锁管理器-tests.md                 （1539 行 · 20 错误码 · flock + FIFO + 死锁）
├── L2-03-审计记录器+追溯查询-tests.md     （1783 行 · 30 错误码 · 19 TC 已建）
├── L2-04-检查点与恢复器-tests.md          （2224 行 · 47 错误码 · 20 TC · RECOVER 分类）
└── L2-05-崩溃安全层-tests.md              （2058 行 · 27 错误码 · CRASH 分类）
```

---

## 必读参考

1. **3-1 接口源**：`docs/3-1-Solution-Technical/L1-08-多模态内容处理/L2-0X-XXX.md` + `docs/3-1-Solution-Technical/L1-09-韧性+审计/L2-0X-XXX.md`
2. **prd**：`docs/2-prd/L1-08 多模态内容处理/prd.md` + `docs/2-prd/L1-09 韧性+审计/prd.md`
3. **G 标杆**：`docs/3-2-Solution-TDD/L1-01-主 Agent 决策循环/L2-02-决策引擎-tests.md`
4. **M5 规划**：`docs/superpowers/reviews/2026-04-22-M5-TDD-parallel-plan.md`
5. **契约**：`docs/3-1-Solution-Technical/integration/ic-contracts.md`（L1-08 涉 IC-11/12 · L1-09 是 IC-09/10/18 的承担方 · **全 L1 都通过 IC-09 写事件 · 最热的契约**）

---

## 硬必填

遵循 **总览 md 的 10 段模板** + 5 会话通用质量标杆。

**TC ID**：
- L1-08 · `TC-L108-L20N-NNN` 或 `TC-<MD|CODE|VISION|PATH>-<TYPE>-NNN`（F 会话参考）
- L1-09 · `TC-L109-L20N-NNN` 或 `TC-<BUS|LOCK|AUDIT|RECOVER|CRASH>-<TYPE>-NNN`（F 会话已建 L2-04/L2-05 用 `T-RECOVER-*` / `TC-CRASH-*` 风格）

**L1-09 特殊**：是系统脊柱 · 所有 L1 都通过 IC-09 写 · 测试必含：
- **系统级 halt 测试**（fsync 失败 · PM-08 单一事实源）
- **hash chain 断裂检测**
- **崩溃恢复 Tier 1-4**
- **跨 session bootstrap**

---

## L2 定位速查

### L1-08 多模态内容处理

| L2 | 核心方法 | 关键错误码 | 关键场景 |
|:---|:---|:---|:---|
| L2-01 文档 IO 编排器 | `read_md` / `write_md` / `edit_md` · 分页 | MD_PAGED_ORDER_BROKEN / MD_POST_WRITE_MISMATCH · frontmatter 校验 | 大 md 分页读 · atomic_write · sha256 复检 |
| L2-02 代码结构理解 | `parse_code` / `build_dep_graph` | TREESITTER_FAIL / AST_TIMEOUT | tree-sitter · 依赖图 · 符号索引 |
| L2-03 图片视觉理解 | `invoke_vlm` / `fallback_to_ocr` | VLM_TIMEOUT / LOW_CONFIDENCE | VLM 调用 · 缓存 · 降级 OCR |
| L2-04 路径安全与降级 | `validate_path` / `fallback_route` | PATH_TRAVERSAL / OUTSIDE_SANDBOX / SYMLINK_LOOP | ALLOWLIST · 沙箱 · 多级降级 · 审计 |

### L1-09 韧性+审计（脊柱 · 按 pid 物理分片）

| L2 | 核心方法 | 关键错误码 | 关键场景 |
|:---|:---|:---|:---|
| L2-01 事件总线核心 | `append_event(event)` | FSYNC_FAIL / HASH_CHAIN_BROKEN | IC-09 入口 · 每 event 必 fsync · 系统级 halt |
| L2-02 锁管理器 | `acquire_lock` / `release_lock` | LOCK_TIMEOUT / DEADLOCK / INVALID_TOKEN | flock + FIFO · 死锁检测 · TTL 泄漏回收 |
| L2-03 审计记录器+追溯 | `record_audit` / `query_trail` | AUDIT_SCHEMA_FAIL / QUERY_TIMEOUT | IC-09 落盘 · rotation · 按 pid/time/actor 查询 |
| L2-04 检查点与恢复器 | `take_snapshot` / `recover_from_checkpoint` / `begin_shutdown` | DEADLINE_EXCEEDED / CHECKPOINT_CORRUPT / PID_MISMATCH | Tier 1-4 · 30s 硬约束 · bootstrap |
| L2-05 崩溃安全层 | `append_atomic` / `write_atomic` / `verify_integrity` | FSYNC_FAIL / HASH_CHAIN_BROKEN / DISK_FULL | WAL · tmpfile+rename · sha256 链 |

---

## 执行节奏（每份 · 6 步）

1. Read 3-1 对应 L2（§3/§11/§13）
2. Read prd 对应小节
3. Read G 标杆
4. Write 整份 tests.md（500-1200 行）
5. Bash 验证
6. Commit 独立：
   ```bash
   git commit -m "feat(harnessFlow): R5.4-K · L1-08/L2-0X XXX tests depth-B"
   # 或 L1-09/
   ```

---

## 推进顺序建议

**先 L1-08（4 份 · 多模态）** L2-04（路径安全 · 底座）→ L2-01 → L2-02 → L2-03

**后 L1-09（5 份 · 脊柱）** L2-05（崩溃安全 · 最底）→ L2-01（事件总线）→ L2-02（锁）→ L2-04（checkpoint）→ L2-03（审计查询）

理由：路径安全/崩溃安全是最底座 · 其他 L2 依赖。L1-09 全链稳定是整个系统的前提。

---

## 最终验收

```bash
python3 <<'PYEOF'
import os, re
for base_dir in ["docs/3-2-Solution-TDD/L1-08-多模态内容处理",
                 "docs/3-2-Solution-TDD/L1-09-韧性+审计"]:
    print(f"=== {base_dir} ===")
    for fn in sorted(os.listdir(base_dir)):
        if not fn.endswith(".md"): continue
        fp = os.path.join(base_dir, fn)
        with open(fp, encoding="utf-8") as f: c = f.read()
        lines = c.count(chr(10))+1
        fill = c.count("<!-- FILL")
        secs = len(re.findall(r"^## §\d", c, re.M))
        tc = len(set(re.findall(r"TC-L\d+-L\d+-\w+|TC-[A-Z]+-[A-Z]+-\d+|T-[A-Z]+-[A-Z]+-\d+", c)))
        tests = len(re.findall(r"\bdef test_", c))
        status = "✓" if (fill == 0 and secs >= 10 and tc >= 15 and tests >= 15) else "✗"
        print(f"{status} {fn}: lines={lines} §={secs} FILL={fill} TC={tc} test_fn={tests}")
PYEOF

./scripts/quality_gate.sh
git push origin main
```

回主会话：**9 份 commit SHA + 总行数**。

---

## 禁区

- 不改 3-1 任何文件
- 不碰其他 L1 tests 目录
- 不碰 integration/ acceptance/ scripts/
- **L1-09 是脊柱 · 测试用例必含 PM-08 单一事实源的各种攻击测试**（fsync 失败 · 盘满 · hash 断链 · race condition）· 不可略

---

## 开工前检查

```bash
git pull origin main
git status
ls "docs/3-2-Solution-TDD/L1-08-多模态内容处理/"
ls "docs/3-2-Solution-TDD/L1-09-韧性+审计/"
```

准备好从 **L1-08/L2-04 路径安全与降级** 开始（所有路径的守门人 · 地基）。
