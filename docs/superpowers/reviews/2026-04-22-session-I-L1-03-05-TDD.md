# 【并发会话 I】3-2 TDD · L1-03 WBS 调度 × 5 + L1-05 Skill 生态 × 5 · 共 10 份

## 背景

harnessFlow 3-2-Solution-TDD 剩 44 份未做 · 主会话 M5 并发分派。
你（I 会话）负责 **L1-03 WBS+WP 拓扑调度 × 5 + L1-05 Skill 生态+子 Agent × 5 = 10 份**。

其他会话并发做 L1-02/06/07/08/09/10，彼此不冲突。

---

## 任务文件（只动 L1-03 + L1-05 两目录）

### L1-03 WBS+WP 拓扑调度（5 份）

```
docs/3-2-Solution-TDD/L1-03-WBS+WP 拓扑调度/
├── L2-01-WBS 拆解器-tests.md              （1354 行 3-1 源 · 14 错误码 · 17 TC）
├── L2-02-拓扑图管理器-tests.md            （1097 行 · 14 错误码 · 17 TC）
├── L2-03-WP 调度器-tests.md               （1239 行 · 13 错误码 · 20 TC）
├── L2-04-WP 完成度追踪器-tests.md         （1301 行 · 17 错误码 · 17 TC）
└── L2-05-失败回退协调器-tests.md          （1598 行 · 13 错误码 · 18 TC）
```

### L1-05 Skill 生态+子 Agent 调度（5 份）

```
docs/3-2-Solution-TDD/L1-05-Skill生态+子Agent调度/
├── L2-01-Skill 注册表-tests.md            （1777 行 3-1 源 · 12 错误码 · 21 TC）
├── L2-02-Skill 意图选择器-tests.md        （1549 行 · 14 分级 · ≥15 TC）
├── L2-03-Skill 调用执行器-tests.md        （1793 行 · 37 错误码 · 多 TC）
├── L2-04-子 Agent 委托器-tests.md         （813 行 · 16 错误码 · 18 TC）
└── L2-05-异步结果回收器-tests.md          （1750 行 · 14 分级 · 多 TC）
```

---

## 必读参考

1. **3-1 接口源**：`docs/3-1-Solution-Technical/L1-03-WBS+WP 拓扑调度/L2-0X-XXX.md` + `docs/3-1-Solution-Technical/L1-05-Skill生态+子Agent调度/L2-0X-XXX.md`
2. **prd 负向用例**：`docs/2-prd/L1-03 WBS+WP 拓扑调度/prd.md` + `docs/2-prd/L1-05 Skill生态+子Agent调度/prd.md`
3. **G 会话标杆**：`docs/3-2-Solution-TDD/L1-01-主 Agent 决策循环/L2-02-决策引擎-tests.md`（学风格）
4. **M5 规划总览**：`docs/superpowers/reviews/2026-04-22-M5-TDD-parallel-plan.md`（10 段模板 + 质量标杆）
5. **契约**：`docs/3-1-Solution-Technical/integration/ic-contracts.md`（L1-03 涉及 IC-02/19 · L1-05 涉及 IC-04/05/12/20）

---

## 硬必填（每份 500-1000 行）

遵循 **总览 md 的 10 段模板**：§0 进度 / §1 覆盖度 / §2 正向 / §3 负向 / §4 IC 契约 / §5 性能 / §6 e2e / §7 fixture / §8 集成点 / §9 边界

**TC ID 命名**：
- L1-03 用 `TC-L103-L20N-NNN` 或 `TC-<WBS|TOPO|SCHED|TRACK|ROLLBACK>-<TYPE>-NNN`
- L1-05 用 `TC-L105-L20N-NNN` 或 `TC-<REG|INTENT|EXEC|SUB|ASYNC>-<TYPE>-NNN`

---

## L2 定位速查

### L1-03 WBS+WP 拓扑调度

| L2 | 核心方法（§3）| 关键错误码（§11）| 关键场景 |
|:---|:---|:---|:---|
| L2-01 WBS 拆解器 | `decompose_wbs` / `diff_merge` | TOPO_CYCLE / UPSTREAM_MISSING / PM14_VIOLATION | 4 件套→WBS 拓扑 · 差量合并 |
| L2-02 拓扑图管理器 | `load_topology` / `snapshot` / `check_cycle` | CYCLE_DETECTED / SNAPSHOT_CORRUPT | DAG 环检测 · 6 状态机 · 关键路径 |
| L2-03 WP 调度器 | `schedule_next_wp` / `transition_state` | CONCURRENT_LIMIT / DEPS_UNMET | 并发度 2 · 拓扑序派发 |
| L2-04 完成度追踪器 | `subscribe_events` / `aggregate_progress` | SSE_TIMEOUT / BURNDOWN_SKEW | 订阅 L1-09 事件 · Burndown |
| L2-05 失败回退协调器 | `coordinate_rollback` | RETRY_EXHAUSTED / ROLLBACK_LOOP | WP 失败 retry/skip/rollback/escalate |

### L1-05 Skill 生态+子 Agent 调度

| L2 | 核心方法（§3）| 关键错误码（§11）| 关键场景 |
|:---|:---|:---|:---|
| L2-01 Skill 注册表 | `query_candidates` / `write_ledger` | SKILL_NOT_FOUND / LEDGER_SCHEMA_MISMATCH | 启动加载 · 热更新 · 账本回写 |
| L2-02 Skill 意图选择器 | `select_chain` / `advance_fallback` | HARD_EDGE_VIOLATION / CAPABILITY_EXHAUSTED | 5 信号混合打分 · 硬编码 scan |
| L2-03 Skill 调用执行器 | `invoke_skill` | SCHEMA_INVALID / TIMEOUT | context 注入 · timeout · retry · audit |
| L2-04 子 Agent 委托器 | `spawn_subagent` / `collect_result` | SPAWN_FAIL / CONTEXT_OVERFLOW | 独立 session · Claude Agent SDK · PM-03 |
| L2-05 异步结果回收器 | `validate_async_return` / `forward` | SCHEMA_MISMATCH / DOD_GATE_REJECTED | 子 Agent 完成事件回收 · DoD 校验 |

---

## 执行节奏（每份 · 6 步）

1. Read 3-1 对应 L2（§3/§11/§13）
2. Read prd 对应小节
3. Read G 标杆（L1-01/L2-02-决策引擎-tests.md）学 fixture 风格
4. Write 整份 tests.md（500-1000 行）
5. Bash 验证
6. Commit（每份独立）：
   ```bash
   git commit -m "feat(harnessFlow): R5.4-I · L1-03/L2-0X XXX tests depth-B"
   # 或
   git commit -m "feat(harnessFlow): R5.4-I · L1-05/L2-0X XXX tests depth-B"
   ```

---

## 推进顺序建议

**先 L1-03（5 份 · 调度链地基）** 按 L2-02（拓扑地基）→ L2-01 → L2-03 → L2-04 → L2-05

**后 L1-05（5 份 · skill 调用链）** 按 L2-01（注册表地基）→ L2-02 → L2-03 → L2-04 → L2-05

理由：L2-02 拓扑图 / L2-01 注册表是各自 L1 的地基 · 其他 L2 都依赖。

---

## 最终验收

```bash
# 1. 自检 10 份
python3 <<'PYEOF'
import os, re
for base_dir in ["docs/3-2-Solution-TDD/L1-03-WBS+WP 拓扑调度",
                 "docs/3-2-Solution-TDD/L1-05-Skill生态+子Agent调度"]:
    print(f"=== {base_dir} ===")
    for fn in sorted(os.listdir(base_dir)):
        if not fn.endswith(".md"): continue
        fp = os.path.join(base_dir, fn)
        with open(fp, encoding="utf-8") as f: c = f.read()
        lines = c.count(chr(10))+1
        fill = c.count("<!-- FILL")
        secs = len(re.findall(r"^## §\d", c, re.M))
        tc = len(set(re.findall(r"TC-L\d+-L\d+-\w+|TC-[A-Z]+-[A-Z]+-\d+", c)))
        tests = len(re.findall(r"\bdef test_", c))
        status = "✓" if (fill == 0 and secs >= 10 and tc >= 15 and tests >= 15) else "✗"
        print(f"{status} {fn}: lines={lines} §={secs} FILL={fill} TC={tc} test_fn={tests}")
PYEOF

# 2. Gate
./scripts/quality_gate.sh

# 3. push
git push origin main
```

完成后回主会话：**10 份全部 depth-B · commit SHA 列表 · 总行数**。

---

## 禁区

- 不改 3-1 任何文件
- 不碰 L1-02/06/07/08/09/10 tests 目录
- 不碰 integration/ 和 acceptance/
- 不碰 scripts/

---

## 开工前检查

```bash
git pull origin main
git status
ls "docs/3-2-Solution-TDD/L1-03-WBS+WP 拓扑调度/"
ls "docs/3-2-Solution-TDD/L1-05-Skill生态+子Agent调度/"
wc -l "docs/3-2-Solution-TDD/L1-01-主 Agent 决策循环/L2-02-决策引擎-tests.md"
```

准备好从 **L1-03/L2-02 拓扑图管理器** 开始。
