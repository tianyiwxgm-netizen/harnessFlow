# 【并发会话 J】3-2 TDD · L1-06 3 层 KB × 5 + L1-07 Harness 监督 × 6 · 共 11 份

## 背景

你（J 会话）负责 **L1-06 3 层知识库 × 5 + L1-07 Harness 监督 × 6 = 11 份**。
其他会话并发做 L1-02/03/05/08/09/10，不冲突。

**11 份是 5 会话中最多的** · 若遇配额限 · 先做 L1-06 × 5 commit 后再做 L1-07 × 6。

---

## 任务文件（只动 L1-06 + L1-07 两目录）

### L1-06 3 层知识库（5 份）

```
docs/3-2-Solution-TDD/L1-06-3层知识库/
├── L2-01-3 层分层管理器-tests.md          （2095 行 3-1 源 · 12 错误码 · IC=101）
├── L2-02-KB 读-tests.md                    （2103 行 · 12+ 错误码）
├── L2-03-观察累积器-tests.md               （2007 行 · 多错误码）
├── L2-04-KB 晋升仪式执行器-tests.md        （2302 行 · 多错误码 · IC=133）
└── L2-05-检索+Rerank-tests.md              （2298 行 · 多错误码）
```

### L1-07 Harness 监督（6 份）

```
docs/3-2-Solution-TDD/L1-07-Harness监督/
├── L2-01-8 维度监督状态采集器-tests.md
├── L2-02-4 级偏差判定器-tests.md
├── L2-03-硬红线拦截器-tests.md
├── L2-04-Supervisor 副 Agent 事件发送器-tests.md
├── L2-05-Soft-drift 模式识别器-tests.md
└── L2-06-死循环升级器+回退路由控制器-tests.md
```

---

## 必读参考

1. **3-1 接口源**：`docs/3-1-Solution-Technical/L1-06-3层知识库/L2-0X-XXX.md` + `docs/3-1-Solution-Technical/L1-07-Harness监督/L2-0X-XXX.md`
2. **prd 负向用例**：`docs/2-prd/L1-06 3层知识库/prd.md` + `docs/2-prd/L1-07 Harness监督/prd.md`
3. **G 标杆**：`docs/3-2-Solution-TDD/L1-01-主 Agent 决策循环/L2-02-决策引擎-tests.md`
4. **M5 规划**：`docs/superpowers/reviews/2026-04-22-M5-TDD-parallel-plan.md`
5. **契约**：`docs/3-1-Solution-Technical/integration/ic-contracts.md`（L1-06 涉 IC-06/07/08 · L1-07 发 IC-13/14/15）

---

## 硬必填

遵循 **总览 md 的 10 段模板** + 5 会话通用质量标杆。

**TC ID**：
- L1-06 · `TC-L106-L20N-NNN` 或 `TC-<KB|TIER|READ|WRITE|PROMOTE|RERANK>-<TYPE>-NNN`
- L1-07 · `TC-L107-L20N-NNN` 或 `TC-<SUP|RED|DRIFT|ESCALATE>-<TYPE>-NNN`

---

## L2 定位速查

### L1-06 3 层知识库

| L2 | 核心方法 | 关键错误码 | 关键场景 |
|:---|:---|:---|:---|
| L2-01 3 层分层管理器 | `tier_of(scope)` / `read_ledger` / `write_ledger` | TIER_MISMATCH / LEDGER_NO_LOCK / LEDGER_SCHEMA_FAIL | session/project/global 三层 · 事件总线同步 · 账本原子写 |
| L2-02 KB 读 | `kb_read(kind, scope, filter)` | KIND_INVALID / SCOPE_VIOLATION / FILTER_EMPTY | IC-06 入口 · session+project+global 默认合并 · rerank |
| L2-03 观察累积器 | `append_observation` / `dedup_check` | DEDUP_COLLISION / OBSERVATION_INVALID | session 层累积 · 去重 · 进 KB 晋升候选 |
| L2-04 KB 晋升仪式 | `promote_to_project` / `promote_to_global` | PROMOTION_REJECTED / SOURCE_NOT_READY | IC-08 入口 · 用户审核 · S7 收尾时触发 |
| L2-05 检索+Rerank | `search` / `rerank` | INDEX_CORRUPT / RERANK_TIMEOUT | BM25 + embedding · LRU 缓存 |

### L1-07 Harness 监督（旁路 · 不阻塞主流）

| L2 | 核心方法 | 关键错误码 | 关键场景 |
|:---|:---|:---|:---|
| L2-01 8 维度监督状态采集器 | `collect_dim_state` / `emit_scan` | DIM_UNKNOWN / SCAN_TIMEOUT | 订阅 L1-09 事件 · 30s 扫描 · 8 维 |
| L2-02 4 级偏差判定器 | `evaluate_deviation` | LEVEL_INVALID / EVIDENCE_NULL | INFO/WARN/FAIL/CRITICAL 分级 |
| L2-03 硬红线拦截器 | `intercept_red_line` | RED_LINE_FALSE_POSITIVE / EVIDENCE_INCOMPLETE | 不可逆操作拦截 · IC-15 阻塞 ≤ 100ms |
| L2-04 事件发送器 | `push_suggestion` / `push_rollback` / `request_halt` | IC_ENQUEUE_FAIL / DOWNSTREAM_REJECT | IC-13/14/15 发送 · 队列背压 |
| L2-05 Soft-drift 模式识别器 | `detect_drift` | PATTERN_UNKNOWN / WINDOW_TOO_SMALL | 温水煮青蛙识别 · 滑窗统计 |
| L2-06 死循环升级器+回退路由 | `escalate_on_repeat` | ESCALATE_SKIP / COUNTER_CORRUPT | 同级 retry ≥ 3 · 升级下一级 |

---

## 执行节奏（每份 · 6 步）

1. Read 3-1 对应 L2（§3/§11/§13）
2. Read prd 对应小节
3. Read G 标杆 fixture
4. Write 整份 tests.md（500-1000 行）
5. Bash 验证
6. Commit 独立：
   ```bash
   git commit -m "feat(harnessFlow): R5.4-J · L1-06/L2-0X XXX tests depth-B"
   # 或 L1-07/
   ```

---

## 推进顺序建议

**先 L1-06（5 份 · 知识底座）** L2-01（3 层地基）→ L2-02 → L2-03 → L2-04 → L2-05

**后 L1-07（6 份 · 监督旁路）** L2-01（采集地基）→ L2-02（判定）→ L2-05（Drift）→ L2-06（升级）→ L2-04（发送）→ L2-03（红线 · 最严格）

理由：L1-06 是 L1-07 的数据来源（L1-07 订阅 IC-09 事件但也读 L1-06）· 先稳 L1-06。

---

## 最终验收

```bash
python3 <<'PYEOF'
import os, re
for base_dir in ["docs/3-2-Solution-TDD/L1-06-3层知识库",
                 "docs/3-2-Solution-TDD/L1-07-Harness监督"]:
    print(f"=== {base_dir} ===")
    for fn in sorted(os.listdir(base_dir)):
        if not fn.endswith(".md"): continue
        fp = os.path.join(base_dir, fn)
        with open(fp, encoding="utf-8") as f: c = f.read()
        lines = c.count(chr(10))+1
        fill = c.count("<!-- FILL")
        secs = len(re.findall(r"^## §\d", c, re.M))
        tc = len(set(re.findall(r"TC-L\d+-L\d+-\w+|TC-[A-Z]+-[A-Z]+-\d+", c)))
        tests = len(re.findall(r"\bdef test_", c))
        status = "✓" if (fill == 0 and secs >= 10 and tc >= 15 and tests >= 15) else "✗"
        print(f"{status} {fn}: lines={lines} §={secs} FILL={fill} TC={tc} test_fn={tests}")
PYEOF

./scripts/quality_gate.sh
git push origin main
```

回主会话：**11 份 commit SHA + 总行数**。

---

## 禁区

- 不改 3-1 任何文件
- 不碰其他 L1 tests 目录
- 不碰 integration/ acceptance/ scripts/

---

## 开工前检查

```bash
git pull origin main
git status
ls "docs/3-2-Solution-TDD/L1-06-3层知识库/"
ls "docs/3-2-Solution-TDD/L1-07-Harness监督/"
```

准备好从 **L1-06/L2-01 3 层分层管理器** 开始。
