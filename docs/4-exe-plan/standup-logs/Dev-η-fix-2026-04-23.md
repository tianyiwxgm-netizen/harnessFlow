---
session: Dev-η-fix
date: 2026-04-23
wp_current: L1-08-review-fix
branch: feat/dev-eta-l1-08
base_sha: 48454b0
head_sha: e569424
---

# Dev-η-fix · 2026-04-23 · review 必修闭环

## 背景

主会话对 `feat/dev-eta-l1-08` 做轻量 review（agent rate limit fallback），输出
`docs/superpowers/reviews/2026-04-23-Dev-η-review.md`：
- **P1-01**（真 bug · 必修）· IC-12 `ts` 硬编码 `"2026-04-23T00:00:00Z"`
- **P1-02**（待验证）· IC-12 dispatch P99 ≤ 200ms SLO 无 benchmark 证据
- **P2-03**（建议批量）· 16 个 ruff B904 raise-from warnings

本会话闭环这 3 项。

## 昨日完成（review 必修）

### P1-01 · IC-12 ts 硬编码 → UTC now

- 文件：`app/multimodal/ic_12_delegator.py`
- 抽出 `_utc_now_iso()` helper · 内部 `datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")`
- cmd 字段 `ts` 从字面量改为 `_utc_now_iso()`
- **commit**：`d0476eb fix(l1-08): IC-12 ts 改 UTC now · 修 P1-01 硬编码 bug`

**新增 3 TC**（`tests/multimodal/integration/test_ic_12_delegator.py`）：
1. `test_ic_12_ts_is_dynamic_utc_now_not_hardcoded` — ISO-8601 Z 正则 + `before <= parsed <= after` 时间窗断言 · 显式断不以 `2026-04-23T00:00:00` 开头（抓住旧 bug）
2. `test_ic_12_ts_differs_across_consecutive_calls` — 2 次连调间 `asyncio.sleep(0.002)` · 断 `ts1 != ts2`
3. `test_ic_12_ts_uses_injected_clock` — `monkeypatch.setattr(ic_12_delegator, "_utc_now_iso", ...)` 固定时钟 · 证明是唯一时间源（便于未来 freezegun 风格测试）

### P1-02 · IC-12 dispatch P99 ≤ 200ms SLO benchmark

- 新文件：`tests/multimodal/perf/test_ic_12_dispatch_slo.py`（115 行）
- **commit**：`4feca43 test(l1-08): IC-12 dispatch P99 SLO benchmark · 修 P1-02 待验证项`

**新增 2 TC**：

1. `test_ic_12_dispatch_p99_under_200ms_bulk`
   - `_FastMockL1_05` async client · 10 次 warmup → 1000 次采样 · `time.perf_counter()` sub-ms 精度
   - 排序取 P50/P95/P99/max/mean · 硬断 `p99 <= 200.0`
   - stdout 打印原始数字供本日志引用

2. `test_ic_12_dispatch_benchmark`
   - `pytest-benchmark` pedantic · `rounds=50 iterations=1 warmup_rounds=5` · 作 CI 侧趋势追踪

**实测 benchmark 数据**（commit `e569424` tip · macOS arm64 · Python 3.11.0 · n=1000）：

| 指标 | 值 | SLO | 余量 |
|---|---|---|---|
| mean | 0.1275 ms | — | — |
| P50 | 0.1143 ms | — | — |
| P95 | 0.1917 ms | — | — |
| **P99** | **0.2866 ms** | **≤ 200 ms** | **~697×** |
| max | 0.3423 ms | — | — |

pytest-benchmark 侧（`pedantic` 50 rounds · 含 `asyncio.run` fixture overhead）：
- mean = 444.0 µs = 0.444 ms
- median = 420.0 µs = 0.420 ms
- min = 386.4 µs

**结论**：IC-12 dispatch P99 与 200ms SLO 距离 2 个数量级以上 · 不是边缘通过。

### P2-03 · 16 处 B904 raise-from 批量修

- **commit**：`e569424 style(l1-08): 16 处 B904 加 raise-from · 保留异常链`
- `ruff --fix` 不自动改 B904（informational · 无 autofix 器）· 逐处手动加 `from e`
- 覆盖 7 个文件 16 处：
  - `doc_io/md_reader.py` ×3
  - `doc_io/md_writer.py` ×4
  - `doc_io/yaml_rw.py` ×3
  - `path_safety/facade.py` ×2
  - `path_safety/lock_keeper.py` ×1
  - `path_safety/whitelist.py` ×2
  - `vision/vlm_invoker.py` ×1

验证：
```
$ ruff check app/multimodal tests/multimodal --select B904
All checks passed!
```

残余 37 个非-B904 风格警告（E402 ×26 / UP017 ×3 / F841 ×2 / …）是 P3 · 本轮范围外 · 不影响交付。

## 今日计划

- push `feat/dev-eta-l1-08` 到 origin（此前未 push）
- 交主会话：3 commit 到手 · 可开 PR 到 main

## 阻塞

- 无。

## DoD 进度

- P1-01: ✅ 3 新 TC 绿 · 绿色回归防硬编码 + 动态 + mock 注入
- P1-02: ✅ 2 新 TC 绿 · P99 = 0.29 ms（SLO 200 ms）· 数字入 standup log
- P2-03: ✅ B904 0（原 16）· 全测 225 绿 · 覆盖率 93.12% ≥ 85%

## 代码产出

- commits: 3 个（d0476eb · 4feca43 · e569424）
- 测试：220 → 225（+5 TC · 3 P1-01 · 2 P1-02）
- 覆盖率：93.1% → 93.12%（持平 · 新增代码即新增 TC）
- B904 warnings：16 → 0
- 文件改动：9 个 src 文件 · 2 个 test 文件
- LOC：+218 / -32

## 关键证据链

1. ts 是否真的 dynamic：`test_ic_12_ts_differs_across_consecutive_calls` 连调 2 次必异
2. P99 数字：直接从 `pytest -s` stdout 抓 — `p99=0.2866ms`
3. B904 归零：`ruff check --select B904` → `All checks passed!`
4. 全量守门：`pytest tests/multimodal/ --cov-fail-under=85` → `225 passed` + `93.12%`

---

*— Dev-η-fix · 2026-04-23 · 3 review 必修闭环 · 待主会话 PR —*
