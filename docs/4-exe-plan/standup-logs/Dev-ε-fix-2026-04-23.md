---
session: Dev-ε (fix · retry)
date: 2026-04-23
L1: L1-03 WBS+WP 拓扑调度
wave: 2
branch: feat/dev-epsilon-l1-03
worktree: .worktrees/dev-epsilon-l1-03
status: done
---

# Dev-ε · Fix · Standup（修 code review 拦的 2 P1）

## TL;DR

**2/2 P1 修完** · +13 新 TC · 共 230 TC 全绿 · cov 保持 94.1% · 已 push 到 origin。

前次 agent 遇到 529 Overloaded 未完成 · 本会话继续推完。

## P1 修复快照

| P1 | 主题 | 改动文件 | commit SHA | 新 TC |
|:---|:---|:---|:---:|:---:|
| P1-01 | EventBusStub IC-09 signature 缺字段 | `app/l1_03/common/event_bus_stub.py` + `tests/l1_03/integration/test_ic_contracts.py` | `b3015da` | +9 |
| P1-02 | FailureCounter 重复 escalation | `app/l1_03/rollback/coordinator.py` + `tests/l1_03/test_l2_05_rollback.py` | `425a30a` | +4（- 1 旧 TC 修订） |

## P1-01 细节

**Bug**：`append(event_type, content, project_id)` 缺 IC-09 §3.9.2 必填的
`event_id / actor / ts`。Dev-α 真实 `EventAppender` 切过来时 12+ 个 `_emit`
调用点都得改。

**修复**（drop-in 兼容）：
- `append()` 扩 6 个 optional kwargs：`payload / event_id / actor / ts /
  trigger_tick / correlation_id`
- stub 宽松 · 未传则生成默认（`evt-{uuid}` / `{"l1": "L1-03"}` / ISO-8601 Z）
- 新增 `append_strict()` · 严格签名 · 缺任一必填 raise · 供真实 IC-09 契约测
- `EventRecord` 扩 `payload / actor / ts / trigger_tick / correlation_id` ·
  `content` 保留为 legacy alias（与 payload 同值 · 老 filter/assert 零改动）
- 出参补 `prev_hash / persisted / ts_persisted`（§3.9.3）
- 暴露 `_IC09_REQUIRED` 白名单（event_id, event_type, project_id, payload,
  actor, ts）

**新 TC（`TestIC09ContractSchema`）**：
1. `_IC09_REQUIRED` 白名单对齐 §3.9.2 required
2. `append()` 接全部契约字段 · 回存对象完整
3. `append_strict()` 缺 `event_id` raise
4. `append_strict()` 缺 `actor.l1` raise
5. `append_strict()` 缺 `ts` raise
6. `append()` 宽松路径 · default 自动填
7. `payload + content` 同时传 → TypeError（互斥）
8. 空 `project_id` → ValueError（PM-14）
9. 现有 `manager.transition_state` → `_emit` drop-in 兼容（全字段回存）

## P1-02 细节

**Bug**：`ESCALATION_STATES = {RETRY_3, ESCALATED}` 让 `on_wp_failed` 在 wp 到
RETRY_3 后每次继续失败（counter 粘 ESCALATED）都走升级分支 · IC-14 被重复 fire ·
L1-04 侧收到 N 份重复 advice。

**修复**：
- `RollbackCoordinator.__init__` 新增 `_escalated_wps: set[str]`
- `on_wp_failed` emit 前锁内检查 `wp_id in _escalated_wps: return` · 首次升级
  把 wp_id 加入 set
- `on_wp_done_reset` 除 counter 清零 · 还 `discard(wp_id)` 解除去重 · 下一轮
  若又连 3 次失败能重新升级（Quality Loop 重跑后再崩溃语义）
- 所有检查在既有 RLock 内 · 并发安全

**新 TC（`TestEscalationDedup`）**：
1. `TC-L103-L205-401` · 10 次 `on_wp_failed` · captured_routes 恰 1 · advice_issued
   event 恰 1 · counter 粘 ESCALATED
2. per-wp dedup（非全局）· `wp-a` / `wp-b` 各自 fire 1 次
3. `on_wp_done_reset` 后再连 3 次失败 · 能再 fire（新 cycle）
4. `MagicMock(spec=Escalator).push_rollback_route.assert_called_once()`

**调整旧 TC** · `test_fourth_failure_keeps_escalated_state_but_no_reemit`：
老期望 "每次都报" 正是 P1-02 bug · 新语义改为 "counter 仍粘 ESCALATED · IC-14
只 fire 1 次"。

## DoD Gate

- [x] TDD：先红后绿（2 组 TC 加进去时 3 红 · 实现后全绿）
- [x] 只改 `app/l1_03/` + `tests/l1_03/`（约束）
- [x] `feat/dev-epsilon-l1-03` 分支上 · 禁用 `git add -A`（全部指定文件）
- [x] 覆盖率 ≥ 85% · **实测 94.1%**
- [x] 全绿：230 passed（+13 新，-0 失败）
- [x] push origin 成功

## 测试增量

| 组 | 原 | +新 | 总 |
|:---:|:---:|:---:|:---:|
| `test_l2_05_rollback.py` | 38 | +4（1 修订不算增）| 42 |
| `integration/test_ic_contracts.py` | — | +9 | +9 |
| **合计** | 217 | **+13** | **230** |

## 覆盖率（cov=app.l1_03）

- `common/event_bus_stub.py` · 保持高覆盖（新分支走 append_strict）
- `rollback/coordinator.py` · 保持 100% line cov（dedup 分支新 TC 覆盖）
- 整组 **94.1%** · 未降

## Push 状态

已 `git push origin feat/dev-epsilon-l1-03` · HEAD 推进到 `425a30a`。

## 收尾

- [x] 2 fix commit
- [x] standup log 落 `docs/4-exe-plan/standup-logs/Dev-ε-fix-2026-04-23.md`
- [x] Push origin
- [ ] 等 code-reviewer 二审 · 确认 2 P1 都关了

---

*— Dev-ε 2026-04-23 fix session · L1-03 WBS+WP 拓扑调度 · 2 P1 修复交付 —*
