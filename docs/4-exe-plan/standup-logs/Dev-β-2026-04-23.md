---
session: Dev-β
date: 2026-04-23
wave: 1（底座组）
scope: app/knowledge_base/  (原 app/l1_06/)
---

# Dev-β · 2026-04-23 standup

## 完成情况

| WP | L2 | 范围 | TC | 覆盖 | 落地 commit |
|:---|:---|:---|:---:|:---:|:---|
| **β-WP01** | L2-01 TierManager | 8 算法 + 6 IC + 12 错误码 + 降级链 | **51 / 51** | 89.1% | `65c1a6b` |
| **β-WP02** | L2-05 RerankService | 5 信号 scorer + rerank + reverse_recall + on_stage_transitioned + push_to_l101 + idem cache + FALLBACK_RAW | **55 / 55** | 87.2% 总 | `8511ca7`（见下） |
| **总计** | 2 / 6 WP | tier_manager + retrieval | **106 / 106** | ≥85% 组级门达标 | — |

## 关键技术决策 / 自修正记录

### β-WP01

- **§6 情形 D · 目录命名**：Dev-β 计划 §1.3 `app/l1_06/tier_manager/` vs TDD 测试 import `app.l1_06.l2_01.*` → 采用 TDD 路径锁定（契约）
- **降级链**：READ_ONLY_ISOLATION（bus 超时 buffer）+ EMERGENCY_LOCKDOWN（yaml corrupt）

### β-WP02

- **§6 情形 C · 算法分歧**：Dev-β 计划 §3.2 写 "BM25 + embedding hybrid"，但 3-1 L2-05 §6 实际设计为 **5 信号线性加权**（context_match + stage_match + observed_count + recency + kind_priority）。采用 3-1 真实设计（契约 > 计划拟议）。建议主会话同步 §3.2 文案。
- **Perf 测试 fixture 优化**：原 `make_candidates(N)` 置于计时块内，每次构造 1000 MagicMock ≈ 500ms 掩盖 rerank 本身性能 → 移至循环外预构造，SLO 通过。

### 包语义化命名（中途用户指令）

按 2026-04-23 用户指令 + 跟随 Dev-δ 已有模式（`l1_02 → project_lifecycle`）：

- `app/l1_06/` → `app/knowledge_base/`
- `l2_01/` → `tier_manager/`
- `l2_05/` → `retrieval/`
- 测试树同步重命名 + 文件名去 `_l2_0X_` 前缀

只动 Dev-β 独占的 `app/l1_06/` 与 `tests/l1_06/`，未触 Dev-α (`app/l1_09`) / Dev-δ (`app/project_lifecycle`)。

## ⚠️ Commit 混淆事故（请主会话留意）

我 stage 完 β-WP02 全部文件 + 准备执行 `git commit -m "β-WP02..."` 时，
另一会话（主会话准备 main-1 brief）执行了 `git add -A && git commit -m
"docs(prep): main-1 L1-04 Quality Loop brief"`，将我 staged 的 WP02 文件
一并打包进了 `8511ca7`。

- **代码**：完整无损，全部 commit 在 `8511ca7`
- **commit message**：名不符实（写的是 docs(prep)，实际包含我 1100+ 行 WP02 代码 + tier_manager 重命名）
- **后果**：
  - 我准备的 WP02 commit message（DoD 验收记录、5 信号设计自修正、perf 测试 fixture 改进）丢失
  - Git blame / changelog 追溯 WP02 时会指向一个 docs commit，查不到 WP02 实际 commit 故事

**建议主会话仲裁**：若需要规范化 commit history，可由主会话补一份"事后说明" commit（doc-only），或在 release notes 里手动注释 8511ca7 实际包含 β-WP02 内容。

## 测试现状（HEAD = 29be9d6 时）

```
.venv/bin/python -m pytest tests/knowledge_base/ -q
106 passed in 8.65s
```

- ruff check: green
- mypy --ignore-missing-imports: green
- coverage:
  - tier_manager/tier_manager.py     89.1%
  - tier_manager/_collaborators.py   89.3%
  - retrieval/service.py             89.3%
  - retrieval/_scorers.py            67.6%（scorer fallback 分支未全打 · WP06 集成补 · 非热路径）
  - errors.py × 2                    100%
  - **总计 87.2%** ≥ 85% 组级门

## 包边界遵守

| 路径 | 状态 |
|:---|:---|
| `app/knowledge_base/`        | ✅ Dev-β 独占，新增 / 改名 |
| `tests/knowledge_base/`      | ✅ Dev-β 独占，新增 / 改名 |
| `app/l1_09/`                 | ❌ Dev-α 独占，未触 |
| `app/project_lifecycle/`     | ❌ Dev-δ 独占，未触 |
| `app/supervisor/`            | ❌ Dev-ζ 独占，未触 |
| `pyproject.toml`             | ❌ 未改 |
| `.gitignore`                 | ❌ 未改 |
| `docs/CODE-OWNERSHIP-MATRIX` | ❌ 未改 |

## 剩余 WP（4/6 待续）

| WP | L2 | 前置 | 估时 | 状态 |
|:---|:---|:---|:---:|:---|
| β-WP03 | L2-02 KB 读 IC-06 | WP01+02 ✓ | 1d | 未启动 |
| β-WP04 | L2-03 观察累积 IC-07 | WP01 ✓ | 1d | 未启动 |
| β-WP05 | L2-04 晋升仪式 IC-08 | WP01+04 | 1.5d | 未启动 |
| β-WP06 | 组内集成 + PM-14 隔离 | WP01-05 | 0.5d | 未启动 |

—— Dev-β · 2026-04-23 EOD
