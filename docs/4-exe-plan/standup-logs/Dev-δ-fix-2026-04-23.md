---
session: Dev-δ-fix
date: 2026-04-23
author: Dev-δ-fix (L1-02 项目生命周期)
base_review: docs/superpowers/reviews/2026-04-23-Dev-δ-review.md
---

# Dev-δ fix 会话 · 2026-04-23

Dev-δ L1-02 review verdict: **CONDITIONAL_APPROVE**。本会话聚焦 2 P1 必修 + 2 P2。

## 会话约束

- feature branch: `fix/dev-delta-review-2026-04-23`（不再直接 push main）
- TDD：先加 TC 跑红 · 实装 · 跑绿 · commit
- 只改 `app/project_lifecycle/` + `tests/project_lifecycle/`
- 覆盖率 ≥ 85%

## 4 Fix 概览

| # | Issue | Fix commit | TC 增量 | Coverage |
|---|-------|-----------|---------|----------|
| P1-01 | IC-01 缺 4 必填字段 | `b2cdac2` | +6 (410-415) | — |
| P1-02 | IC-16 push_stage_gate_card 未实现 | `6289a92` | +9 (420-428) | — |
| P2-01 | IC-19 payload 缺字段 + command_id 格式 | `3dc8257` | +7 (660-666) | — |
| P2-04 | WP03 补 15 TC + 3 错误码真实 raise + _cross_ref_check | `921b4dc` | +15 (801-805/811-815/821-825) + 4 stub unit (430-433) | 89.05% |
| **合计** | | **4 commits** | **157 → 198 (+41)** | **89.05% ≥ 85%** |

## P1-01 · IC-01 补齐 §3.1.2 4 必填字段

**源**：`ic-contracts.md §3.1.2` required = [transition_id, project_id, from, to,
reason, trigger_tick, evidence_refs, ts]。Dev-δ WP04 call site 只传 5 字段，Protocol
也未声明 4 字段。集成 L1-01 时接口将失配。

**Fix** (`app/project_lifecycle/stage_gate/controller.py`)：

- `L1_01_StateMachine` Protocol 新增 `transition_id`、`trigger_tick`、
  `evidence_refs`、`ts` 4 形参声明
- `authorize_transition` 自动生成：
  - `transition_id = f"trans-{uuid.uuid4()}"` · 幂等键
  - `ts = datetime.now(timezone.utc).isoformat(timespec="microseconds").replace("+00:00", "Z")`
  - `trigger_tick` 默认 `tick-{gate_id}` 派生
  - `evidence_refs` 默认 `(gate_id,)` · 空 tuple raise `E_TRANSITION_FORBIDDEN`

**TC 覆盖** (410-415)：

- 410 `transition_id` 字段 + trans- 前缀
- 411 `trigger_tick` 字段
- 412 `evidence_refs` minItems=1
- 413 `ts` ISO-8601 Z 后缀
- 414 空 `evidence_refs` raise
- 415 `transition_id` 每次 call 唯一

## P1-02 · IC-16 push_stage_gate_card 实装

**源**：`ic-contracts.md §3.16` 定义 Stage Gate 批准后推 UI 卡片。WP04 遗漏此 IC 完全
未实现 · Gate pass 后无 UI 交互链路。

**Fix**：

1. 新增 `app/project_lifecycle/stage_gate/ic_16_stub.py`：
   - `UIBridge` Protocol · Dev-θ L1-10 真实 UI 接入点
   - `build_push_stage_gate_card_command()` 构造 §3.16.2 6 必填字段 payload
   - stage → artifact_type 默认映射（S1→charter · S2→charter/plan/wbs/togaf_doc · …）
2. `StageGateController.__init__` 新增 `ui_bridge: UIBridge | None = None` 可选 DI
3. `request_gate_decision` 内 `decision == "pass"` 分支调 `_push_gate_card`：
   - `ui_bridge` 注入 → delegate `push_stage_gate_card_to_ui`
   - 未注入 → 降级为 IC-09 事件 `ic_16_push_stage_gate_card`（审计可追溯）
4. `need_input` / `reject` 不推（§3.16.1）

**TC 覆盖** (420-428 + 430-433 stub unit)：

- 420 pass 时 push 被调
- 421 6 必填字段齐
- 422 `artifacts_bundle` ≥ 1 + 每项 schema
- 423 `allowed_decisions` 3 值 + `blocks_progress=True`
- 424 need_input 分支不 push
- 425 无 ui_bridge 不 crash（向后兼容）
- 426 `ts` ISO-8601 Z
- 427 无 ui_bridge 发 IC-09 事件
- 428 事件 payload 字段齐
- 430-433 builder 4 校验分支（gate_id/project_id/bundle/trim_level 异常）

## P2-01 · IC-19 补齐 §3.19.2

**源**：`ic-contracts.md §3.19.2` required = [command_id, project_id, artifacts_4_pack,
architecture_output]。Dev-δ WP03 原实现只传 3 字段 · `command_id=f"wbs-{project_id}"`
格式错误且不唯一（§3.19.5 non-idempotent）。

**Fix** (`app/project_lifecycle/four_set/producer.py`)：

- `request_wbs_decomposition` 新增入参：
  - `artifacts_4_pack: dict` required 4 子字段（charter/plan/requirements/risk_path）
  - `architecture_output: dict` required 子字段（togaf_phases/adr_path）
  - `target_wp_granularity: str = "medium"`
- `command_id = f"wbs-req-{uuid.uuid4()}"` · §3.19.5 每次新值
- `ts` ISO-8601 utc Z
- 缺字段 raise `ValueError`（对齐 E_WBS_4_PACK_INCOMPLETE / E_WBS_ARCH_OUTPUT_MISSING）

**TC 覆盖** (660-666)：

- 660 command_id UUID 格式
- 661 每次调用唯一
- 662 artifacts_4_pack 4 字段齐
- 663 architecture_output 2 字段齐
- 664 ts ISO-8601
- 665 缺 artifacts_4_pack → raise
- 666 缺 architecture_output → raise

老 TC 606 升级：传完整 4_pack + arch + 检查 `wbs-req-` 前缀。

## P2-04 · WP03 补 15 TC + 3 错误码真实 raise + `_cross_ref_check` 实装

**源**：3-1 tech-design §5.2 / §6.3 / §6.4 定义的 E04 `E_CROSS_REF_DEAD` · E09
`E_REDO_OUT_OF_SCOPE` · E14 `E_DEPENDENCY_CLOSURE_EMPTY` 三错误码 Dev-δ 只 import 未 raise
· 主会话裁定 **"补实现"**（不偷省）。

**Fix** (`app/project_lifecycle/four_set/producer.py`)：

1. 新增 `_DEPENDENCY_CLOSURE` 重做级联矩阵（§6.4）：
   - `requirements` → 全 4 级联
   - `goals` → {goals, ac, qs}
   - `acceptance_criteria` → {ac, qs}
   - `quality_standards` → {qs}
2. `assemble_four_set` 新增 redo 分支（`target_subset` 非空即 redo）：
   - `_resolve_dependency_closure()` · 非法 doc_type raise E14 + 返合法列表
   - `_load_baseline_items()` · 从 manifest.yaml 读取 baseline
   - `_check_redo_scope()` · closure 外 doc 变更 raise E09
3. `_cross_ref_check(is_redo)` 语义区分：
   - `is_redo=False` 初始装配 → 保留原 `E_TRACEABILITY_BROKEN` (E03)
   - `is_redo=True` 重做 → 新 `E_CROSS_REF_DEAD` (E04) · 上游已删 · 下游仍引

**TC 覆盖** (15 TC)：

- `E_CROSS_REF_DEAD` (801-805)：
  - 801 goal→deleted req
  - 802 ac→deleted goal
  - 803 初始装配时仍用 E03（语义区分）
  - 804 qs→deleted ac
  - 805 审计 `context["dead_refs"]` 含 ID 明细
- `E_DEPENDENCY_CLOSURE_EMPTY` (811-815)：
  - 811 非法 doc_type
  - 812 错误返回 context 含合法列表
  - 813 单合法 doc_type 通过
  - 814 全 4 doc_type 通过
  - 815 混合合法+非法 严格 raise
- `E_REDO_OUT_OF_SCOPE` (821-825)：
  - 821 target=qs 但改了 req
  - 822 审计 context 点明越界
  - 823 in-scope 变更通过
  - 824 target=ac closure={ac,qs} · 动 req raise
  - 825 初始装配不触发（只 redo 时检测）

## 验证

```bash
pytest tests/project_lifecycle/ --cov=app.project_lifecycle --cov-fail-under=85 -q
```

结果：

```
198 passed in 10.28s
Required test coverage of 85% reached. Total coverage: 89.05%
```

- 全绿 ≥ 198（157 → 198 · +41 TC）
- 覆盖率 89.05% ≥ 85%（Dev-δ baseline 88.4% 上浮 0.65 pp）

## commit trail

```
b2cdac2 fix(L1-02 P1-01): IC-01 补齐 §3.1.2 4 必填字段
6289a92 fix(L1-02 P1-02): 实装 IC-16 push_stage_gate_card
3dc8257 fix(L1-02 P2-01): IC-19 补齐 §3.19.2 必填字段 + 唯一 command_id
921b4dc fix(L1-02 P2-04): WP03 补 15 TC + 3 错误码真实 raise + _cross_ref_check 实装
```

## 未处理项

- P2-02 closing/producer.py 4 个 unused import（ruff F401） —  非 review prompt 必修项
- P2-03 mypy 2 个 Literal 类型错误 — 非 review prompt 必修项
- P3-01/02/03/04/05 代码质量改进 — 非 review prompt 必修项

所有未处理项均为 review 报告标注的 LOW 优先 · 不阻断 L1-01 集成 · 留后续 WP 或独立 linting 清理会话处理。

## 分支状态

- 分支：`fix/dev-delta-review-2026-04-23`
- base: `main`
- 4 commits on this branch + 1 unrelated β docs commit (`b9eadc4`) 被 rebase 上流自动带入
- 准备 push origin
