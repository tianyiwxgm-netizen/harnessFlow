---
session: Dev-α (α1 fix · review 修拦)
date: 2026-04-23
L1: L1-09 韧性+审计
wave: 1
branch: fix/dev-alpha-α1-review
worktree: /tmp/hf_fix_alpha1（主 worktree 被 Dev-β fix agent 并发占用 · 用独立 worktree 隔离）
status: done
---

# Dev-α · α1 Fix · Standup（修 code review 拦的 1 CRITICAL + 4 HIGH + 4 P2）

## TL;DR

**9/9 fix 修完** · +25 新 TC · 135 → 160 TC（+25）· 全绿 · cov 保持 91.6% · branch 已可 push。

Review `docs/superpowers/reviews/2026-04-23-Dev-α-α1-review.md` verdict: BLOCK → **UNBLOCK**（全部 P0/P1/P2 修完）.

## 修复快照

| Rank | ID | 主题 | 改动文件 | commit SHA | 新 TC |
|:---|:---|:---|:---|:---:|:---:|
| CRITICAL | B-1 | BusDiskFull 缺 halt_guard.mark_halt() | `event_bus/core.py` + `test_review_alpha1_fixes.py` | `b4a8181` | +3 |
| HIGH | B-2 | save_meta 失败必须 halt（防 stale meta） | `event_bus/core.py` | `d03a70f` | +2 |
| HIGH | B-3 | `_truncate_jsonl` fsync 失败不得 suppress | `crash_safety/integrity_checker.py` | `82fba30` | +1 |
| HIGH | B-4 | AssertionError → LineTooLargeError | `crash_safety/appender.py` | `9584d7d` | +2 |
| HIGH | E-1 | request_context 正确 reset Token · 不擦外层 | `event_bus/context.py` | `e284e49` | +3 |
| P2 | A-4 | sequence 从 1 起（§3.9.3 minimum: 1） | `event_bus/schemas.py` + 4 test files | `40d1970` | +5（+ 5 旧 TC 修订） |
| P2 | A-1 | AppendEventResult 字段名对齐 §3.9.3 | `event_bus/schemas.py` + `event_bus/core.py` | `45cd764` | +2（+ 1 旧 TC 扩展） |
| P2 | A-2 | Event 加 trigger_tick（§3.9.2 optional） | `event_bus/schemas.py` + `event_bus/core.py` | `a240025` | +3 |
| P2 | A-3 | project_id 扩允 "system" 保留值 | `event_bus/schemas.py` | `497db55` | +4 |

## B-1 CRITICAL 细节

**Bug**：`DiskFullError` 分支仅 raise BusDiskFull · 不调 `halt_guard.mark_halt()`.
后果: halt.marker 文件未落盘 → 跨进程 / 新 EventBus 实例不知 halt 状态 · 审计链断裂.

**修复**:
- `except DiskFullError` 分支加 `halt_guard.mark_halt + self._state = BusState.HALTED`
- `except OSError as exc` 兜底 ENOSPC 分支同理 halt
- 3 新 TC: `halt_guard.is_halted()` / `halt.marker` 落盘 / 跨 bus 实例持久

**对照 review 原要求**:
- [x] DiskFullError 分支也调 halt_guard.mark_halt 再 raise
- [x] TC: 触发 disk full · 验证 halt_guard.is_halted() == True + halt.marker 文件存在 + 新 EventBus 实例启动时检测到 halt

## B-2 HIGH 细节

**Bug**：save_meta 调用在 try/except 外 · FsyncFailed propagate 为 raw CrashSafetyError ·
事件已落盘但 meta 脏 · 下次 append 用 stale last_sequence → 同 seq 复用 → hash 链断.

**修复**:
- save_meta 包在 `try/except FsyncFailed`
- 失败 → `halt_guard.mark_halt` + `BusFsyncFailed` 以映射响应面 4
- 其他 CrashSafetyError 也 halt + BusWriteFailed
- 2 新 TC: FsyncFailed 触发 halt + halt 后续 append 拒（防 seq reuse）

**对照 review 原要求**:
- [x] save_meta 包在 try/except
- [x] FsyncFailed → BusError + halt_guard.mark_halt + raise
- [x] TC: mock save_meta raise FsyncFailed · 验证 halt 触发 + event 已写但 sequence 不 advance（halt 后续拒）

## B-3 HIGH 细节

**Bug**：`_truncate_jsonl` 用 `contextlib.suppress(OSError)` 包 os.fsync · 违反
L2-05 fsync-no-retry 铁律. truncate 后 fsync 失败 silently 返 · 文件未持久化截断.

**修复**:
- 删 `contextlib.suppress` 包装 · fsync 失败 → raise FsyncFailed(errno, target)
- import contextlib 删除（不再用）
- 1 新 TC: mock fsync raise → 必抛 FsyncFailed（不是默默返回）

## B-4 HIGH 细节

**Bug**：PIPE_BUF_LIMIT 超长 · raise AssertionError. 3 问题:
1. AssertionError 不在 CrashSafetyError 体系 · except CrashSafetyError 漏
2. Python -O 吞 assert · 4KB+ 超长行 silently pass → 破 O_APPEND 原子性
3. AssertionError 无 halt_system 标志 · halt_guard 不可见

**修复**:
- raise `LineTooLargeError(...)` 取代 AssertionError（LineTooLargeError 已存 schemas.py）
- TC-108 改 `pytest.raises(LineTooLargeError)` · 不再允许 AssertionError
- 2 新 TC: 确认 LineTooLargeError + 确认继承 CrashSafetyError

## E-1 HIGH 细节

**Bug**：`request_context` finally 块无条件 `set(None)` 三个 ContextVar ·
Token 记录的前值完全忽略. 嵌套场景下：

```python
with request_context(correlation_id="outer"):
    with request_context(correlation_id="inner"):
        pass
    # outer 被擦为 None（bug）
```

L1-01 主循环 tick-level correlation_id 被 L2-01 嵌套 request_context 擦 · 追溯链断.

**修复**:
- 改收集 `(ContextVar, Token)` 配对 list
- finally 块 reverse 遍历 · 调 `var.reset(tok)` 恢复前值
- 3 新 TC: nested correlation / nested trace+span / pre-existing preserved

## A-1 P2 细节

**Bug**：IC-09 §3.9.3 要求 ts_persisted (str ISO-8601) + storage_path + persisted=True ·
实装 `persisted_at: datetime` + `file_path` + 缺 persisted. 下游 IC-10 / IC-18 按 §3.9.3
字段名引用 · 跨 spec YAML bridging 会 schema fail.

**修复**:
- AppendEventResult 新字段 ts_persisted / storage_path / persisted
- 旧字段 persisted_at / file_path 保留为 `@property` alias（deprecated · V2 移除）
  避免破坏已 merge 其他 Dev 组调用代码
- core.py append() 构造用新字段名
- 2 新 TC + 更新 TestContract.test_append_event_result_schema_stable

## A-2 P2 细节

**修复**：Event schema 加 `trigger_tick: str | None` with ULID pattern.
core.py `_event_to_body` 传递 · 写入 body 参与 hash 链（不可篡改追溯）.

## A-3 P2 细节

**修复**：Event.project_id 原 pattern 直接 Field 约束 · 改为 field_validator ·
允许 `"system"` 保留值（halt / startup / crash-recovery 系统级事件需此值）.
非法仍被拒（ValueError）· 不开口子.

## A-4 P2 细节

**修复**：
- ProjectMeta.last_sequence 默认 -1 → 0 · ge=0
- AppendEventResult.sequence ge=0 → ge=1
- 更新 5 处既有测试期望值（sequence 起 1）：`test_first_append_seq_one_prev_genesis` / `test_consecutive_append_seq_monotonic` / `test_meta_persisted` / `test_multi_project_independent_seq` / `test_is_meta_flag` / `test_clear_halt_unlocks` / `test_read_full_range` / `test_read_with_from_seq` / `test_read_with_to_seq` / `test_read_large_streaming` / `test_append_triggers_callback`

## Test 统计

- 135 → 160 TC（+25 新 TC）
- 修订 ~11 既有 TC 期望值（seq=0 → seq=1）
- 全绿 in 4.80s
- coverage 91.6%（ge 85% 门槛 · 保持水平）

```
app/l1_09/crash_safety/appender.py               81      2     24      2  96.2%
app/l1_09/crash_safety/atomic_writer.py         121     10     34      5  90.3%
app/l1_09/crash_safety/canonical_json.py          7      0      0      0 100.0%
app/l1_09/crash_safety/hash_chain.py             24      2      0      0  91.7%
app/l1_09/crash_safety/integrity_checker.py     166     10     48      4  92.5%
app/l1_09/event_bus/context.py                   42      0     12      1  98.1%
app/l1_09/event_bus/core.py                     156     16     36      5  88.0%
app/l1_09/event_bus/halt_guard.py                73     10     12      3  84.7%
app/l1_09/event_bus/meta.py                      28      3      4      1  87.5%
app/l1_09/event_bus/reader.py                    42      5     20      1  87.1%
app/l1_09/event_bus/subscriber.py                78      0     18      0 100.0%
TOTAL                                           818     58    208     22  91.6%
```

## 环境挑战

主 worktree（`/Users/zhongtianyi/work/code/harnessFlow`）并发运行 Dev-β fix agent ·
频繁 git checkout 切换到 `feat/dev-beta-wp03-06` · 会覆盖我的 local changes.

对策：`git worktree add /tmp/hf_fix_alpha1 fix/dev-alpha-α1-review` · 独立隔离 · 全程
在独立 worktree 工作 · commit 也落在 fix 分支.

## 后续

- Dev-α α2 批（WP07-13 lock manager + replay subscriber 等）可继续 · 拦点已清
- 下游 Dev-β fix agent（同时运行）若用真实 IC-09 stub · 字段 alias 保留 · signature 稳定
- A-1 的 `persisted_at` / `file_path` deprecation warning 待 V2 统一清理
- 对照 review 遗留 E-2 (P2 read_range `BusProjectNotRegistered` for empty project)
  本次未修 · 可并入 α2 批（非 block）
